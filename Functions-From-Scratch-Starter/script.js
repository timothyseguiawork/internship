function basic(){
  console.log(`basic()`);
}
function singlePara(para){
  console.log(`singleParam(para): ${para}`);
}
function twoPara(para, para2) {
  console.log(`singleParam(para): ${para}, ${para2}`);
}
